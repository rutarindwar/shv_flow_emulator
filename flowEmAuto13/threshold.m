% Find a threshold for measuring distance between contour lines
clc;
dirname = './contours';
c = 0;
for i = 1:1953
    name = sprintf('%s/contours40_%06d.txt', dirname, i);
    
    if (exist(name, 'file') == 0)
        
    end

end

